#include <stdio.h>
#define MAX 20
void squeeze(char s1[MAX], char s2[MAX]) {
    int i, j, k;
    for (i = k = 0; s1[i] != '\0'; i++) {
    	found=0;
    	for (i = 0; s2[j] != '\0'; i++) {
    		if(s1[i]==s2[j]){
    			found=1
    			s1[i++]
    		}

    }
}
